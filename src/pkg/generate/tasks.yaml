includes:
  - setup: https://raw.githubusercontent.com/defenseunicorns/uds-common/main/tasks/setup.yaml
  - create: https://raw.githubusercontent.com/defenseunicorns/uds-common/main/tasks/create.yaml
  - deploy: https://raw.githubusercontent.com/defenseunicorns/uds-common/main/tasks/deploy.yaml

tasks:
  - name: default
    actions:
      - task: setup-cluster
      - task: create-package
      - task: deploy-package

  - name: setup-cluster
    actions:
      - task: setup:k3d-test-cluster
  
  - name: create-package
    actions:
      - task: create:package

  - name: deploy-package
    actions:
      - task: deploy:package
