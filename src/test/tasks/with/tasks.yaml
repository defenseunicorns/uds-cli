tasks:
  - name: echo
    inputs:
      echo:
        default: new york
        description: The thing to echo
    actions:
      - cmd: |
          echo "${{ .inputs.echo }} says hello"
        description: "Echo ${{ .inputs.echo }}"

  - name: hello
    inputs:
      hello:
        default: los angeles
        description: The thing to say hello to
    actions:
      # - cmd: |
      #     echo "FOO=bar" >> .env
      #     cat .env >> $UDS_ENV
      #     rm .env
      #   mute: true
      # - cmd: env | grep FOO
      - task: echo
        with:
          echo: ${{ .inputs.hello }}
      # - cmd: |
      #     echo "${INPUT_HELLO}"
