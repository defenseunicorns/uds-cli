kind: UDSBundle
metadata:
  name: local-init
  description: building from a local init pkg
  version: 0.0.1

zarf-packages:
  - name: init
    path: "../zarf"
    ref: "###BNDL_TMPL_INIT_VERSION###"
    optional-components:
      - git-server
