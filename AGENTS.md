For documentation changes under `docs/`, read `.agents/skills/documentation-authoring/SKILL.md` before editing.

`AGENTS.md` is the repository's general instruction file. Repository-specific procedural guidance belongs in `.agents/skills/` or `.agents/rules/`.
